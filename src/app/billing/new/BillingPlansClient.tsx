"use client";
import { useState } from "react";

type BillingTerm = "monthly" | "quarterly" | "semiannual" | "annual";
type Price = { priceCents: number; currency: string };
type Plan = { id: string; name: string; description: string | null; priceCents: number; currency: string; prices: Record<BillingTerm, Price | null>; billingInterval: "MONTH" | "YEAR"; modules: string[] };
type Guild = { id: string; name: string; discordGuildId: string; planName: string | null; active: boolean; expiresAt: string | null; providerCustomerId: string | null; cancelAtPeriodEnd: boolean };
type Complimentary = { freeMonths: number; remainingGuilds: number };
const TERMS: { id: BillingTerm; label: string; months: number; discount: number }[] = [{ id: "monthly", label: "1 month", months: 1, discount: 0 }, { id: "quarterly", label: "3 months", months: 3, discount: 10 }, { id: "semiannual", label: "6 months", months: 6, discount: 15 }, { id: "annual", label: "1 year", months: 12, discount: 25 }];

export default function BillingPlansClient({ plans, ownedGuildCount, guilds, complimentary }: { plans: Plan[]; ownedGuildCount: number; guilds: Guild[]; complimentary: Complimentary | null }) {
  const renewableGuilds = guilds.filter((guild) => !guild.active); const activeGuilds = guilds.filter((guild) => guild.active);
  const [selectedPlan, setSelectedPlan] = useState(plans[0]?.id ?? ""); const [billingTerm, setBillingTerm] = useState<BillingTerm>("monthly"); const [selectedGuildId, setSelectedGuildId] = useState(""); const [guildName, setGuildName] = useState(""); const [discordGuildId, setDiscordGuildId] = useState(""); const [referralCode, setReferralCode] = useState(""); const [referralValid, setReferralValid] = useState<boolean | null>(null); const [referralDiscount, setReferralDiscount] = useState(10); const [referralMessage, setReferralMessage] = useState(""); const [loading, setLoading] = useState(false); const [portalGuildId, setPortalGuildId] = useState(""); const [error, setError] = useState("");
  const renewing = selectedGuildId !== ""; const selectedGuild = guilds.find((guild) => guild.id === selectedGuildId); const selectedTerm = TERMS.find((term) => term.id === billingTerm)!;
  const selectedPlanData = plans.find((plan) => plan.id === selectedPlan); const selectedPrice = selectedPlanData?.prices[billingTerm] ?? (selectedPlanData ? { priceCents: selectedPlanData.priceCents * selectedTerm.months, currency: selectedPlanData.currency } : null);
  const listedTotal = selectedPrice?.priceCents ?? 0; const affiliateAmount = referralValid && !renewing ? Math.round(listedTotal * referralDiscount / 100) : 0; const checkoutTotal = billingTerm === "monthly" && referralValid !== true ? Math.round(listedTotal / 2) : listedTotal - affiliateAmount;
  function selectGuild(id: string) { setSelectedGuildId(id); const guild = guilds.find((item) => item.id === id); if (guild) { setGuildName(guild.name); setDiscordGuildId(guild.discordGuildId); } else { setGuildName(""); setDiscordGuildId(""); } setReferralCode(""); setReferralValid(null); setReferralMessage(""); setError(""); }
  async function validateReferral() { const code = referralCode.trim(); if (!code) { setReferralValid(null); setReferralMessage(""); return; } setReferralValid(null); setReferralMessage("Checking referral code…"); try { const response = await fetch("/api/billing/checkout", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ validateReferralCode: code }) }); const data = await response.json() as { valid?: boolean; affiliateName?: string; discountPercent?: number; error?: string }; if (response.ok && data.valid) { const discount = data.discountPercent ?? 10; setReferralDiscount(discount); setReferralValid(true); setReferralMessage(`${discount}% off every month${data.affiliateName ? ` · ${data.affiliateName}` : ""}`); } else { setReferralValid(false); setReferralMessage("Referral code is invalid or inactive."); } } catch { setReferralValid(false); setReferralMessage("Unable to validate the referral code."); } }
  async function openPortal(guildId: string) { setError(""); setPortalGuildId(guildId); try { const response = await fetch("/api/billing/portal", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ guildId }) }); const data = await response.json() as { url?: string; error?: string }; if (!response.ok || !data.url) throw new Error(data.error ?? "Unable to open subscription management."); window.location.assign(data.url); } catch (e) { setError(e instanceof Error ? e.message : "Unable to open subscription management."); setPortalGuildId(""); } }
  async function submit(path: string) { setError(""); if (!selectedPlan) { setError("Choose a plan."); return; } if (!renewing && (!guildName.trim() || !discordGuildId.trim())) { setError("Enter your guild name and Discord Guild ID."); return; } setLoading(true); try { const response = await fetch(path, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ planId: selectedPlan, billingTerm, ...(renewing ? { guildId: selectedGuildId } : { guildName: guildName.trim(), discordGuildId: discordGuildId.trim(), ...(referralCode.trim() ? { referralCode: referralCode.trim() } : {}) }) }) }); const data = await response.json() as { checkoutUrl?: string; error?: string }; if (!response.ok) throw new Error(data.error === "INVALID_REFERRAL_CODE" ? "That referral code is invalid or inactive." : data.error ?? "Unable to update the subscription."); if (data.checkoutUrl) window.location.assign(data.checkoutUrl); else window.location.assign("/"); } catch (e) { setError(e instanceof Error ? e.message : "Unable to update the subscription."); setLoading(false); } }
  return <div className="text-white">
    <header className="max-w-3xl"><p className="text-sm font-medium uppercase tracking-widest text-zinc-500">ROO Guild Suite</p><h1 className="mt-3 text-4xl font-bold tracking-tight">Subscriptions</h1><p className="mt-3 text-zinc-400">Manage your guild subscriptions. You currently own {ownedGuildCount} guild{ownedGuildCount === 1 ? "" : "s"}.</p></header>
    {activeGuilds.length > 0 && <section className="mt-8 rounded-2xl border border-zinc-800 bg-zinc-900 p-6"><h2 className="text-lg font-semibold">Current subscriptions</h2><p className="mt-1 text-sm text-zinc-500">Your active guilds are renewed automatically. Use Manage subscription to update payment details, change or cancel the subscription.</p><div className="mt-4 space-y-3">{activeGuilds.map((guild) => <div key={guild.id} className="flex flex-col gap-4 rounded-xl border border-zinc-800 bg-zinc-950 p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-semibold">{guild.name}</p><p className="mt-1 text-sm text-zinc-500">{guild.planName ?? "Subscription"}{guild.expiresAt ? ` • Renews ${new Date(guild.expiresAt).toLocaleDateString()}` : ""}{guild.cancelAtPeriodEnd ? " • Scheduled to cancel" : ""}</p></div><button disabled={portalGuildId === guild.id || !guild.providerCustomerId} onClick={() => openPortal(guild.id)} className="rounded-lg border border-zinc-700 px-4 py-2 text-sm font-medium text-white hover:border-zinc-500 disabled:cursor-not-allowed disabled:opacity-50">{portalGuildId === guild.id ? "Opening…" : "Manage subscription"}</button></div>)}</div></section>}
    {renewableGuilds.length > 0 && <section className="mt-8 rounded-2xl border border-amber-800/60 bg-amber-950/20 p-6"><h2 className="text-lg font-semibold text-amber-200">Renew an existing guild</h2><p className="mt-1 text-sm text-amber-100/70">Select an expired or cancelled guild to restore access.</p><select value={selectedGuildId} onChange={(event) => selectGuild(event.target.value)} className="mt-4 w-full rounded-lg border border-zinc-700 bg-zinc-950 px-4 py-3 text-white"><option value="">Select a guild…</option>{renewableGuilds.map((guild) => <option key={guild.id} value={guild.id}>{guild.name}{guild.planName ? ` — ${guild.planName}` : ""}</option>)}</select>{selectedGuild && <p className="mt-2 text-xs text-zinc-500">Discord Guild ID: {selectedGuild.discordGuildId}</p>}</section>}
    <section className="mt-8 rounded-2xl border border-zinc-800 bg-zinc-900 p-6"><h2 className="text-lg font-semibold">Choose your billing term</h2><p className="mt-1 text-sm text-zinc-500">Choose a term to see its exact checkout price.</p><div className="mt-4 grid grid-cols-2 gap-3 md:grid-cols-4">{TERMS.map((term) => <button key={term.id} type="button" onClick={() => setBillingTerm(term.id)} className={`cursor-pointer rounded-xl border p-4 text-left transition ${billingTerm === term.id ? "border-white bg-zinc-800" : "border-zinc-800 bg-zinc-950 hover:border-zinc-600"}`}><p className="font-semibold">{term.label}</p><p className="mt-1 text-xs text-zinc-500">{term.discount ? `${term.discount}% off` : "Standard price"}</p></button>)}</div></section>
    <section className="mt-4 grid gap-4 md:grid-cols-2">{plans.map((plan) => { const isSelected = selectedPlan === plan.id; const price = plan.prices[billingTerm] ?? { priceCents: plan.priceCents * selectedTerm.months, currency: plan.currency }; const isMonthly = billingTerm === "monthly"; const isTotal = plan.name.toLowerCase().includes("total"); const shownPrice = isMonthly && referralValid !== true ? Math.round(price.priceCents / 2) : isMonthly && referralValid === true ? Math.round(price.priceCents * (1 - referralDiscount / 100)) : price.priceCents; return <button key={plan.id} type="button" onClick={() => setSelectedPlan(plan.id)} className={`cursor-pointer rounded-2xl border p-6 text-left transition ${isSelected ? "border-white bg-zinc-800" : "border-zinc-800 bg-zinc-900 hover:border-zinc-600"}`}><div className="flex items-start justify-between gap-4"><div><h2 className="text-xl font-semibold">{plan.name}</h2>{isTotal && <span className="mt-2 inline-flex rounded-full bg-white px-2.5 py-1 text-xs font-semibold text-black">Everything included</span>}</div><div className="text-right"><p className="text-xs text-zinc-500">{selectedTerm.label}</p><p className="text-2xl font-bold">{formatPrice(shownPrice, price.currency)}</p><p className="text-xs text-zinc-500">{isMonthly ? referralValid === true ? `${referralDiscount}% off every month` : <> <span className="line-through">{formatPrice(price.priceCents, price.currency)}</span> · first month 50% off</> : <>{formatPrice(Math.round(price.priceCents / selectedTerm.months), price.currency)}/month</>}</p></div></div><p className="mt-4 text-sm leading-6 text-zinc-400">{plan.description ?? "Guild management tools for your community."}</p><div className="mt-5 space-y-2 text-sm">{plan.modules.map((module) => <p key={module} className="text-zinc-300">✓ {formatModule(module)}</p>)}{plan.modules.length === 1 && plan.modules[0] === "CORE" && <p className="text-zinc-500">— Resources, Bidding & Allocation not included</p>}</div></button>; })}</section>
    {!renewing && <section className="mt-8 rounded-2xl border border-zinc-800 bg-zinc-900 p-6"><h2 className="text-lg font-semibold">New guild details</h2><div className="mt-5 grid gap-5 md:grid-cols-2"><label className="block"><span className="text-sm text-zinc-400">Guild name</span><input value={guildName} onChange={(event) => setGuildName(event.target.value)} maxLength={100} placeholder="My ROO Guild" className="mt-2 w-full rounded-lg border border-zinc-700 bg-zinc-950 px-4 py-3 text-white outline-none focus:border-zinc-400" /></label><label className="block"><span className="text-sm text-zinc-400">Discord Guild ID</span><input value={discordGuildId} onChange={(event) => setDiscordGuildId(event.target.value.replace(/\D/g, ""))} inputMode="numeric" maxLength={20} placeholder="123456789012345678" className="mt-2 w-full rounded-lg border border-zinc-700 bg-zinc-950 px-4 py-3 text-white outline-none focus:border-zinc-400" /></label></div><div className="mt-5 max-w-md"><label className="block"><span className="text-sm text-zinc-400">Referral code <span className="text-zinc-600">(optional)</span></span><div className="mt-2 flex gap-2"><input value={referralCode} onChange={(event) => { setReferralCode(event.target.value.toUpperCase().replace(/\s/g, "")); setReferralValid(null); setReferralMessage(""); }} onBlur={validateReferral} onKeyDown={(event) => { if (event.key === "Enter") { event.preventDefault(); void validateReferral(); } }} maxLength={50} placeholder="Enter influencer code" className={`min-w-0 flex-1 rounded-lg border bg-zinc-950 px-4 py-3 text-white uppercase outline-none ${referralValid === true ? "border-emerald-700" : referralValid === false ? "border-red-800" : "border-zinc-700 focus:border-zinc-400"}`} /><button type="button" onClick={() => void validateReferral()} disabled={!referralCode.trim()} className="cursor-pointer rounded-lg border border-zinc-700 px-4 py-3 text-sm font-medium hover:border-zinc-500 disabled:cursor-not-allowed disabled:opacity-40">Apply</button></div>{referralMessage && <p className={`mt-2 text-xs ${referralValid === true ? "text-emerald-400" : referralValid === false ? "text-red-400" : "text-zinc-500"}`}>{referralValid === true ? "✓ " : ""}{referralMessage}</p>}{referralValid === true && <div className="mt-3 rounded-lg border border-emerald-900/60 bg-emerald-950/20 px-3 py-2 text-sm text-emerald-200">Referral discount applied: <strong>{referralDiscount}% off every month</strong></div>}<p className="mt-2 text-xs text-zinc-500">Have an influencer referral code? Enter it here to receive the affiliate discount every month.</p></label></div>{complimentary && <div className="mt-5 rounded-xl border border-emerald-800/60 bg-emerald-950/30 p-4"><p className="font-semibold text-emerald-200">Complimentary access available</p><p className="mt-1 text-sm text-emerald-100/70">As an approved guild creator, you can create {complimentary.remainingGuilds} more guild{complimentary.remainingGuilds === 1 ? "" : "s"} with {complimentary.freeMonths} month{complimentary.freeMonths === 1 ? "" : "s"} complimentary access.</p><button disabled={loading || !plans.length} onClick={() => submit("/api/billing/complimentary")} className="mt-4 w-full rounded-lg bg-emerald-200 px-5 py-3 font-semibold text-zinc-950 hover:bg-emerald-100 disabled:cursor-not-allowed disabled:opacity-50">{loading ? "Creating guild…" : "Create with complimentary access"}</button></div>}</section>}
    {renewing && <section className="mt-8 rounded-2xl border border-zinc-800 bg-zinc-900 p-6"><p className="text-sm text-zinc-400">Renewing <span className="font-semibold text-white">{selectedGuild?.name}</span>. Its existing Discord Guild ID and ownership will be retained.</p></section>}
    {error && <p className="mt-4 rounded-lg border border-red-900 bg-red-950/30 px-4 py-3 text-sm text-red-300">{error}</p>}
    {!renewing && <section className="mt-6 rounded-2xl border border-zinc-800 bg-zinc-900 p-6"><div className="flex items-center justify-between gap-4"><div><p className="text-sm text-zinc-500">Checkout total</p><p className="mt-1 text-2xl font-bold">{formatPrice(checkoutTotal, selectedPrice?.currency ?? "usd")}</p>{affiliateAmount > 0 && <p className="mt-1 text-xs text-emerald-400">Referral discount: −{formatPrice(affiliateAmount, selectedPrice?.currency ?? "usd")}</p>}</div><button disabled={loading || !selectedPlan} onClick={() => submit("/api/billing/checkout")} className="cursor-pointer rounded-lg bg-white px-6 py-3 font-semibold text-black hover:bg-zinc-200 disabled:cursor-not-allowed disabled:opacity-50">{loading ? "Opening checkout…" : "Continue to checkout →"}</button></div></section>}
  </div>;
}

function formatPrice(cents: number, currency: string) { return new Intl.NumberFormat("en-US", { style: "currency", currency: currency.toUpperCase() }).format(cents / 100); }
function formatModule(module: string) { return module === "CORE" ? "Core Guild Management" : module === "RESOURCE_SUITE" ? "Resources, Bidding & Allocation" : module; }
