import { NextResponse } from "next/server";
import { getCurrentAuth, hasPermission } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import { JOBS } from "@/lib/constants/jobs";
import { announceConfirmedCall, announceFilledCall, ensureCallTables, newId, refreshCallStatus, requirementMatchesJob } from "@/lib/call-to-arms";

type CallRequirement = { id: string; requirement: string; quantity: number };
type ActiveParticipant = { requirement_id: string; job: string | null };

async function getCall(id: string, guildId: string) { const rows = await prisma.$queryRawUnsafe<any[]>(`SELECT c.id, c.title, c.description, c.start_at AS "startAt", c.status, c.creator_user_id AS "creatorUserId", u.username AS "creatorUsername", COALESCE((SELECT SUM(quantity)::integer FROM "guild_call_requirements" WHERE call_id = c.id),0)::integer AS "requiredCount", COALESCE((SELECT COUNT(*)::integer FROM "guild_call_participants" WHERE call_id = c.id AND status = 'ACTIVE'),0)::integer AS "activeCount" FROM "guild_calls" c JOIN "User" u ON u.id = c.creator_user_id WHERE c.id = $1 AND c.guild_id = $2`, id, guildId); return rows[0] ?? null; }
function canManage(call:any, auth:any){return call.creatorUserId===auth.user.id||["ADMIN","MANAGER","OFFICER"].includes(auth.role)}

export async function GET(_: Request, { params }: { params: Promise<{ id: string }> }) { const auth = await getCurrentAuth(); if (!auth) return NextResponse.json({ error: "Authentication required." }, { status: 401 }); if (!hasPermission(auth.role, "events.view")) return NextResponse.json({ error: "You do not have permission to view this call." }, { status: 403 }); try { await ensureCallTables(); const { id } = await params; const call = await getCall(id, auth.guild.id); if (!call) return NextResponse.json({ error: "Call not found." }, { status: 404 }); const requirements = await prisma.$queryRawUnsafe<CallRequirement[]>(`SELECT id, requirement, quantity FROM "guild_call_requirements" WHERE call_id = $1 ORDER BY created_at`, id); const participants = await prisma.$queryRawUnsafe<any[]>(`SELECT p.id, p.requirement_id AS "requirementId", p.status, p.signed_up_at AS "signedUpAt", p.member_id AS "memberId", p.job, m."characterName" AS "characterName", m."discordUsername" AS "discordUsername", m."discordUserId" AS "discordUserId" FROM "guild_call_participants" p JOIN "GuildMember" m ON m.id = p.member_id WHERE p.call_id = $1 ORDER BY p.signed_up_at`, id); const currentMember = await prisma.guildMember.findFirst({ where: { guildId: auth.guild.id, userId: auth.user.id, active: true }, select: { id: true } }); const webhook = await prisma.$queryRawUnsafe<Array<{ webhook_url: string }>>(`SELECT webhook_url FROM "guild_call_webhooks" WHERE guild_id = $1`, auth.guild.id); const manager = canManage(call,auth); return NextResponse.json({ call, requirements, participants, signedUp: participants.some((p) => p.memberId === currentMember?.id), webhookConfigured: Boolean(webhook[0]), canManage: manager }); } catch (error) { console.error("[CALL TO ARMS] detail failed", error); return NextResponse.json({ error: "Failed to load Call To Arms. Please try again." }, { status: 500 }); } }

export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { const auth = await getCurrentAuth(); if (!auth) return NextResponse.json({ error: "Authentication required." }, { status: 401 }); if (!hasPermission(auth.role, "events.view")) return NextResponse.json({ error: "You do not have permission to use calls." }, { status: 403 }); try { await ensureCallTables(); const { id } = await params; const call = await getCall(id, auth.guild.id); if (!call) return NextResponse.json({ error: "Call not found." }, { status: 404 }); const body = await request.json(); const action = body.action;
if (action === "edit") { if (!canManage(call,auth)) return NextResponse.json({ error: "Only the creator or guild managers can edit this call." }, { status: 403 }); if (["CANCELLED","COMPLETED"].includes(call.status)) return NextResponse.json({ error: "This call can no longer be edited." }, { status: 400 }); const title=String(body.title??"").trim(); const description=body.description==null?null:String(body.description); const startAt=String(body.startAt??""); const requirements=Array.isArray(body.requirements)?body.requirements:[]; if(!title||!startAt||!requirements.length)return NextResponse.json({error:"Name, time and at least one job requirement are required."},{status:400}); const parsedDate=new Date(startAt); if(Number.isNaN(parsedDate.getTime())||parsedDate<=new Date())return NextResponse.json({error:"Call time must be a valid future date."},{status:400}); const clean=requirements.map((r:any): CallRequirement=>({id:String(r.id||""),requirement:String(r.requirement||"").trim(),quantity:Number(r.quantity)})); if(clean.some((r:CallRequirement)=>!r.requirement||!Number.isInteger(r.quantity)||r.quantity<1||!JOBS.includes(r.requirement as any)&&r.requirement!=="DPS"))return NextResponse.json({error:"Each requirement must use a valid job or DPS and have at least 1 slot."},{status:400}); const existing=await prisma.$queryRawUnsafe<CallRequirement[]>(`SELECT id, requirement, quantity FROM "guild_call_requirements" WHERE call_id=$1 ORDER BY created_at`,id); const active=await prisma.$queryRawUnsafe<ActiveParticipant[]>(`SELECT requirement_id, job FROM "guild_call_participants" WHERE call_id=$1 AND status='ACTIVE'`,id); for(const p of active){const next=clean.find((r:CallRequirement)=>r.id===p.requirement_id); if(!next||!requirementMatchesJob(next.requirement,p.job))return NextResponse.json({error:"Cannot change or remove a filled job slot while a participant is assigned to it. Ask the participant to leave first."},{status:400}); const old=existing.find((r:CallRequirement)=>r.id===p.requirement_id); if(old&&next.quantity<active.filter((x:ActiveParticipant)=>x.requirement_id===p.requirement_id).length)return NextResponse.json({error:"A requirement cannot be reduced below its current number of participants."},{status:400});}
await prisma.$executeRawUnsafe(`UPDATE "guild_calls" SET title=$2,description=$3,start_at=$4,status='OPEN',announced_at=NULL,reminder_sent_at=NULL,updated_at=now() WHERE id=$1`,id,title,description,parsedDate); const keepIds=new Set(clean.map((r:CallRequirement)=>r.id).filter(Boolean)); for(const r of existing){if(r.id&&!keepIds.has(r.id)){await prisma.$executeRawUnsafe(`DELETE FROM "guild_call_requirements" WHERE id=$1 AND call_id=$2`,r.id,id)}} for(const r of clean){if(r.id&&existing.some((x:CallRequirement)=>x.id===r.id)){await prisma.$executeRawUnsafe(`UPDATE "guild_call_requirements" SET requirement=$2,quantity=$3 WHERE id=$1 AND call_id=$4`,r.id,r.requirement,r.quantity,id)}else{await prisma.$executeRawUnsafe(`INSERT INTO "guild_call_requirements" (id,call_id,requirement,quantity) VALUES ($1,$2,$3,$4)`,newId("requirement"),id,r.requirement,r.quantity)}} await refreshCallStatus(id); return NextResponse.json({ok:true}); }
const member = await prisma.guildMember.findFirst({ where: { guildId: auth.guild.id, userId: auth.user.id, active: true }, select: { id: true, job: true, characterName: true, discordUsername: true, discordUserId: true } }); if (!member) return NextResponse.json({ error: "You need an active guild member profile to sign up." }, { status: 400 });
if (action === "signup") { if (["CANCELLED", "COMPLETED"].includes(call.status)) return NextResponse.json({ error: "This call is no longer open." }, { status: 400 }); const requirementId = String(body.requirementId ?? ""); const selectedJob = String(body.job ?? "").trim(); const reqRows = await prisma.$queryRawUnsafe<Array<{ id: string; requirement: string; quantity: number }>>(`SELECT id, requirement, quantity FROM "guild_call_requirements" WHERE id = $1 AND call_id = $2`, requirementId, id); const requirement = reqRows[0]; if (!requirement) return NextResponse.json({ error: "That requirement is no longer available." }, { status: 400 }); if (!JOBS.includes(selectedJob as (typeof JOBS)[number])) return NextResponse.json({ error: "Please select a valid job." }, { status: 400 }); if (!requirementMatchesJob(requirement.requirement, selectedJob)) return NextResponse.json({ error: `Selected job does not qualify for the ${requirement.requirement} slot.` }, { status: 400 }); if ((await prisma.$queryRawUnsafe<any[]>(`SELECT id FROM "guild_call_participants" WHERE call_id = $1 AND member_id = $2`, id, member.id)).length) return NextResponse.json({ error: "You are already signed up for this call." }, { status: 409 }); const activeCount = await prisma.$queryRawUnsafe<Array<{ count: bigint }>>(`SELECT COUNT(*) FROM "guild_call_participants" WHERE call_id = $1 AND requirement_id = $2 AND status = 'ACTIVE'`, id, requirementId); const status = Number(activeCount[0]?.count ?? 0) < requirement.quantity ? "ACTIVE" : "WAITLIST"; await prisma.$executeRawUnsafe(`INSERT INTO "guild_call_participants" (id, call_id, requirement_id, member_id, job, status) VALUES ($1,$2,$3,$4,$5,$6)`, newId("participant"), id, requirementId, member.id, selectedJob, status); const next = await refreshCallStatus(id); if (next === "FILLED") { try { await announceFilledCall(id); } catch (error) { console.error("[CALL TO ARMS] announcement failed", error); } } return NextResponse.json({ status, notificationAttempted: next === "FILLED" }); }
if (action === "leave") { await prisma.$executeRawUnsafe(`DELETE FROM "guild_call_participants" WHERE call_id = $1 AND member_id = $2`, id, member.id); const reqs = await prisma.$queryRawUnsafe<any[]>(`SELECT id, quantity FROM "guild_call_requirements" WHERE call_id = $1`, id); for (const req of reqs) { const active = await prisma.$queryRawUnsafe<Array<{ count: bigint }>>(`SELECT COUNT(*) FROM "guild_call_participants" WHERE call_id = $1 AND requirement_id = $2 AND status = 'ACTIVE'`, id, req.id); const slots = Math.max(0, Number(req.quantity) - Number(active[0]?.count ?? 0)); if (slots > 0) { const waiters = await prisma.$queryRawUnsafe<Array<{ id: string }>>(`SELECT id FROM "guild_call_participants" WHERE call_id = $1 AND requirement_id = $2 AND status = 'WAITLIST' ORDER BY signed_up_at LIMIT $3`, id, req.id, slots); for (const waiter of waiters) await prisma.$executeRawUnsafe(`UPDATE "guild_call_participants" SET status = 'ACTIVE', updated_at = now() WHERE id = $1`, waiter.id); } } await refreshCallStatus(id); return NextResponse.json({ ok: true }); }
if (action === "cancel" || action === "confirm") { if (!canManage(call,auth)) return NextResponse.json({ error: "Only the creator or guild managers can do this." }, { status: 403 }); const status = action === "cancel" ? "CANCELLED" : "CONFIRMED"; await prisma.$executeRawUnsafe(`UPDATE "guild_calls" SET status = $2, updated_at = now() WHERE id = $1`, id, status); if (action === "confirm") { try { await announceConfirmedCall(id); } catch (error) { console.error("[CALL TO ARMS] confirmation announcement failed", error); } } return NextResponse.json({ ok: true, status }); } return NextResponse.json({ error: "Unknown action." }, { status: 400 }); } catch (error) { console.error("[CALL TO ARMS] action failed", error); return NextResponse.json({ error: "Call To Arms action failed. Please try again." }, { status: 500 }); } }